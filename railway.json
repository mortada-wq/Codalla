{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @workspace/codalla run build"
  },
  "deploy": {
    "startCommand": "pnpm --filter @workspace/codalla run serve"
  }
}
